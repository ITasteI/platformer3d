using UnityEngine;

// Marker component: platforms carrying this are ICY - the player's acceleration/deceleration drop
// sharply while standing on them (see PlayerController), so momentum carries and stopping slides.
// The Eiskristall zone's signature hazard.
public class IceSurface : MonoBehaviour
{
}
